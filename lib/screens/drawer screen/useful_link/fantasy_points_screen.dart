import 'package:flutter/material.dart';

class FantasyPointScreen extends StatelessWidget {
  const FantasyPointScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          "Comming Soon Screen",
          style: TextStyle(fontSize: 35),
        ),
      ),
    );
  }
}
